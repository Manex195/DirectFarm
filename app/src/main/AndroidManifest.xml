<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DirectFarm"
        tools:targetApi="31">

        <!-- Splash / Main Activity -->
        <activity
            android:name=".ui.common.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.DirectFarm">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth Activities -->
        <activity android:name=".ui.auth.LoginActivity" />
        <activity android:name=".ui.auth.RegisterActivity" />

        <!-- Farmer Activities -->
        <activity android:name=".ui.farmer.FarmerDashboardActivity" />
        <activity android:name=".ui.farmer.AddProductActivity" />
        <activity android:name=".ui.farmer.FarmerOrdersActivity" />

        <!-- Customer Activities -->
        <activity android:name=".ui.customer.CustomerDashboardActivity" />
        <activity android:name=".ui.customer.ProductListActivity" />
        <activity android:name=".ui.customer.ProductDetailActivity" />
        <activity android:name=".ui.customer.CustomerOrdersActivity" />

    </application>

</manifest>